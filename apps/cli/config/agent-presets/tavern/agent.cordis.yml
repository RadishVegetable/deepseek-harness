# The Tavern profile supplies its per-session character card and World Info
# context through the host-side Tavern composition. The preset stays a tool policy.

- id: tool-ask-user
  name: '@deepseek-ai/dsh-tool-ask-user'
